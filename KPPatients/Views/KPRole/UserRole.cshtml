
@{
    ViewData["Title"] = "UserRole";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>UserRole</h1>
@if (ViewData["Users"] != null)
{

        <form method="post" asp-action="AddUserRole">
            <div asp-validation-summary="All" class="text-danger"></div>
            @*<input type="hidden" value="@ViewBag.RoleId" asp-for="RoleId" />
            <div class="form-group">
                <label>Users:</label>
                <select asp-for="UserId" class="form-control" asp-items="ViewBag.Users"></select>
            </div>
            *@<div class="form-group">
                <button type="submit" class="btn btn-primary">Add to Role</button>
            </div>
        </form>

        <table class="table">
            <thead>
                <tr>
                    <th>
                        Username
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
            @foreach (var user in ViewBag.UsersRole)
            {
                        <tr>
                            <td>
                        @user.UserName
                            </td>

                    @*@if (SignInManager.IsSignedIn(User))
                    {
                        if (user.UserName != User.Identity.Name && ViewBag.RoleName.ToLower() != "administrators")
                        {
                                            <td>
                                                <a asp-controller="KPRole" asp-action="RemoveUserRole" asp-route-RoleId="@ViewBag.RoleId" asp-route-UserId="@user.UserId">Remove</a>
                                            </td>
                        }
                    }
*@
                        </tr>
            }
        </table>

        <a asp-controller="KPRole" asp-action="RoleList">Return to Role List</a>
}
